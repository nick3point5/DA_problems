import { Node } from "../../4_nodes_list/1/Node/Node.js"

/**
 * Prepends a new node with the given value at the beginning of the linked list.
 *
 * @param {Node} node - The head node of the linked list.
 * @param {number} value - The value to be prepend.
 * @return {Node} - The updated head node of the linked list.
 * @example
 * prepend_one([2], 1) => [1, 2]
 * prepend_one([1], 2) => [2, 1]
 * prepend_one([3], 1) => [1, 3]
 */
export function prepend_one(node: Node, value: number) {
	return null
}
