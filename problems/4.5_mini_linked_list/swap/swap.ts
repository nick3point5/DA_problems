import { Node } from "../../4_nodes_list/1/Node/Node.js"

/**
 * Swaps the given node with its next node.
 *
 * @param {Node} node - the node to be swapped
 * @return {Node} the new head of the swapped nodes
 * @example
 * swap([1, 2]) => [2, 1]
 * swap([2, 1]) => [1, 2]
 * swap([1, 3]) => [3, 1]
 */
export function swap(node: Node) {
	return null
}
