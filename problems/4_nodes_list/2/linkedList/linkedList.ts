import { Node } from "../../1/Node/Node.ts"

/**
 * Creates a linked list from an array.
 *
 * @param {T[]} array - The array of numbers to be converted into a linked list.
 * @return {Node<T>} - The head of the linked list.
 */
export function linkedList<T>(array) {
	return null
}
