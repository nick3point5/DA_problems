/**
 * Adds one to the given number.
 *
 * @param {number} x - The number to add one to.
 * @return {number} The result of adding one to the given number.
 */
export function addOne(x) {
	return x + 1
}
