import { Node } from "../../4_nodes_list/1/Node/Node.ts"

/**
 * Appends two linked lists together.
 *
 * @param {Node | null} list1 - The first linked list.
 * @param {Node | null} list2 - The second linked list.
 * @return {Node | null} The appended linked list.
 * @example
 * appendLists(
 *   [1, 2, 3],
 *   [4, 5, 6]
 * ) => 1 -> 2 -> 3 -> 4 -> 5 -> 6
 */
export function appendLists() {
	return null
}
