import { Node } from "../../4_nodes_list/1/Node/Node.js"

/**
 * Pushes a new node with the given value to the end of the linked list.
 *
 * @param {Node} node - The head of the linked list.
 * @param {number} value - The value to be pushed to the linked list.
 * @return {Node} - The updated head of the linked list.
 * @example
 * push_one(Node(1), 2) => 1 -> 2
 * push_one(Node(1), 3) => 1 -> 3
 * push_one(Node(2), 1) => 1 -> 3
 */
export function push_one(node: Node, value: number) {
	return null
}
