/**
 * GraphNode class with the specified value, neighbors, and visited state.
 *
 * @property {T} value - The value of the graph node.
 * @property {GraphNode<T>[]} - The neighbors of the graph node. Defaults to an empty array.
 * @property {boolean} - The visited state of the graph node. Defaults to false.
 * @property {number} - The distance of the graph node. Defaults to 0.
 * @property {GraphNode<T> | null} - The previous node in the shortest path. Defaults to null.
 */
export class graphNode {
	constructor() {
	}
}
