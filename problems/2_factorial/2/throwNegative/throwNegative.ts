/**
 * Throws an error if the input number is negative, otherwise returns the input number.
 *
 * @param {number} n - The input number.
 * @return {number} The input number if it is not negative.
 * @throws {Error} Throws an error if the input number is negative.
 * @example
 * ```
 * throwNegative(1) = 1
 * throwNegative(0) = 0
 * throwNegative(-1) = Error
 * ```
 */
export function throwNegative() {
	return null
}
