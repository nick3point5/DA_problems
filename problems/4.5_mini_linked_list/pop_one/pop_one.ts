import { Node } from "../../4_nodes_list/1/Node/Node.js"

/**
 * Removes the next node from the given node and returns it.
 *
 * @param {Node} node - the node from which to remove the next node
 * @return {Node} the removed next node
 * @example
 * pop_one([1, 2]) => 2
 * pop_one([2, 1]) => 1
 * pop_one([1, 3]) => 3
 */
export function pop_one(node: Node) {
	return null
}
