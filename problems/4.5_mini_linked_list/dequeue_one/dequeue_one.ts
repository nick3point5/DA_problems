import { Node } from "../../4_nodes_list/1/Node/Node.js"

/**
 * Dequeues (removes and return the head) one node from the given node.
 *
 * @param {Node} node - the node to dequeue from
 * @return {Node} the dequeued node
 * @example
 * dequeue_one([1, 2]) => 1
 * dequeue_one([2, 3]) => 2
 */
export function dequeue_one(node: Node) {
	return null
}
